# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(avalon_simulationTaskLib)
ADD_LIBRARY(${AVALON_SIMULATION_TASKLIB_NAME} SHARED 
    ${AVALON_SIMULATION_TASKLIB_SOURCES} Simulation.cpp)

add_dependencies(${AVALON_SIMULATION_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${AVALON_SIMULATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${AVALON_SIMULATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${AVALON_SIMULATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${AVALON_SIMULATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${AVALON_SIMULATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${AVALON_SIMULATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/avalon_simulation)

